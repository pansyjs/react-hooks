// 此文件在构建时会自动更新，请勿手动修改! 
import useBoolean from './useBoolean';
import useModal from './useModal';
import useToggle from './useToggle';

export {
  useBoolean,
  useModal,
  useToggle
};

export default {
  useBoolean,
  useModal,
  useToggle
};